#!/bin/bash
: ${TM_GO:=go}
require_cmd "$TM_GO"

case "$*" in
--version)
	exec "$TM_GO" version
	;;
esac

if [[ -d "$TM_PROJECT_DIRECTORY" ]]; then
	cd "$TM_PROJECT_DIRECTORY"
elif [[ -d "$TM_DIRECTORY" ]]; then
	cd "$TM_DIRECTORY"
fi
