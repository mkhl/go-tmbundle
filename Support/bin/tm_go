#!/bin/bash

require_cmd go

case "$*" in
--version)
	exec go version
	;;
esac

if [[ -d "$TM_PROJECT_DIRECTORY" ]]; then
	cd "$TM_PROJECT_DIRECTORY"
elif [[ -d "$TM_DIRECTORY" ]]; then
	cd "$TM_DIRECTORY"
fi
